listeners.tcp.default = 5672

# Import broker topology (vhosts, users/ACLs, exchange, queue, binding) on boot.
definitions.import_backend = local_filesystem
definitions.local.path = /etc/rabbitmq/definitions.json